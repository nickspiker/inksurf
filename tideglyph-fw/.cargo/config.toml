# nRF52840 = Cortex-M4F. Overrides the repo-root config (RP2040 thumbv6m) for builds run inside this crate. Link args (--nmagic, -Tlink.x, -Tdefmt.x) are emitted by build.rs.
[build]
target = "thumbv7em-none-eabihf"

[env]
DEFMT_LOG = "info"
