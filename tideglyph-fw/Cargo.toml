[package]
name = "tideglyph-fw"
version = "0.1.0"
edition = "2021"
publish = false
description = "nRF52840 firmware for the cordless BWRY tide clock — embassy + trouble BLE, JD79667 panel"

# Dependency set proven working on this board 2026-07-23 (BLE advertising on air).
# Mirrors trouble repo examples/nrf52 @ c21b1239fb86de712fc99da803def88378d81177.
[dependencies]
embassy-executor = { version = "0.10", default-features = false, features = ["platform-cortex-m", "executor-thread", "defmt", "executor-interrupt"] }
embassy-time = { version = "0.5", default-features = false, features = ["defmt", "defmt-timestamp-uptime"] }
embassy-nrf = { version = "0.11", default-features = false, features = ["defmt", "time-driver-rtc1", "gpiote", "unstable-pac", "rt", "nrf52840"] }
embassy-futures = "0.1.1"
embassy-sync = { version = "0.8", features = ["defmt"] }

futures = { version = "0.3", default-features = false, features = ["async-await"] }
nrf-sdc = { version = "0.4", default-features = false, features = ["defmt", "peripheral", "central", "nrf52840"] }
nrf-mpsl = { version = "0.3", default-features = false, features = ["defmt", "critical-section-impl"] }
bt-hci = { version = "0.9", default-features = false, features = ["defmt"] }
trouble-host = { git = "https://github.com/embassy-rs/trouble", rev = "c21b1239fb86de712fc99da803def88378d81177", features = ["derive", "scan", "gatt"] }

defmt = "1.0"
defmt-rtt = "1.0"
panic-probe = { version = "1.0", features = ["print-defmt"] }

cortex-m = { version = "0.7.6" }
cortex-m-rt = "0.7.0"
static_cell = "2"
blake3 = { version = "1", default-features = false }

# OTA: chip-agnostic embassy-boot core (generic over NorFlash) fed the app's own
# Nvmc — 0.7 pairs with the embassy-sync 0.8 / embassy-embedded-hal 0.6 the app
# already locks, so no second embassy-nrf/embassy-sync gets pulled in.
embassy-boot = { version = "0.7", default-features = false, features = ["defmt"] }
embassy-embedded-hal = { version = "0.6", default-features = false }
embedded-storage-async = "0.4"

# Trust: keyed-BLAKE3 MAC (symmetric, 256-bit shared secret) — NOT Ed25519.
# vsf here is only the manifest PARSER (default-features=false = no crypto-verify,
# so ed25519-dalek/curve25519 are not pulled: ~130 KB saved). The auth is
# blake3::keyed_hash over the image, carried in the manifest's gH field. vsf's
# parser uses alloc, hence embedded-alloc.
vsf = { path = "../../vsf", default-features = false }
embedded-alloc = "0.6"

# build.rs computes the Eagle-time build-stamp floor via vsf (std, host-side).
[build-dependencies]
vsf = { path = "../../vsf" }

[patch.crates-io]
nrf-sdc = { git = "https://github.com/alexmoon/nrf-sdc.git", rev = "abe49d22ebfc85fae134eb082fbfddf752016a55" }
nrf-mpsl = { git = "https://github.com/alexmoon/nrf-sdc.git", rev = "abe49d22ebfc85fae134eb082fbfddf752016a55" }

[profile.release]
debug = 2
lto = "fat"
codegen-units = 1
